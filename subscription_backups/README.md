# 订阅备份

脚本每次成功下载并校验订阅后，都会在此目录保存一份独立的 Clash YAML 配置和 JSON 元数据快照。文件名为 UTC 保存时间加账号哈希，因此历史备份不会被覆盖。YAML 文件可在 Clash Verge 的配置文件导入功能中手动导入；JSON 记录保存时间、流量用量、到期时间和原始订阅地址。

文件名使用账号邮箱的哈希，不会直接暴露账号名称。该目录中的 YAML、JSON 和订阅地址均属敏感信息；请仅保存在可信设备上，勿提交、分享或上传到公共位置。
